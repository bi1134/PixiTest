export enum GuessAction {
    Higher = "Higher",
    Lower = "Lower",
    Skip = "Skip",
    Start = "Start",
}

export enum GuessResult {
    Win = "Win",
    Lose = "Lose",
    Skip = "Skip",
}

export enum GameState {
    NonBetting = "NonBetting",
    Betting = "Betting",
}
